name: dolfyn-dev
channels:
  - defaults
dependencies:
  - pip
  - python>=3.8
  - pytest
  - pip:
      - numpy>=1.21
      - scipy>=1.7.0
      - xarray>=0.19.0
      - netCDF4>=1.5.8
      - bottleneck
